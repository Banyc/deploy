{
  "version": 1,
  "jobs": [
    {
      "id": "oujeQe7WQA",
      "name": "Target ownership groups",
      "description": "Target ownership groups",
      "schedule": "2026-08-25T16:59:38.472Z",
      "scheduleType": "once",
      "subagent_type": "general-purpose",
      "prompt": "SCHEDULED TASK — you will fire after the master's current integration batch has settled. Working directory: /Users/charliesmith/code/deploy (the ROOT checkout). Your dedicated jj workspace already exists: /tmp/code/agent/jj-parallel-features/ws-target-groups (treat it as your repo root when implementing; the root is read-only reference for you).\n\nSTEP 0 — SETTLEMENT CHECK (before ANY implementation): the master is mid-way through a large batch of merges; this task must only start once the trunk is clean. Verify: (a) `cd /Users/charliesmith/code/deploy && jj resolve --list` is EMPTY (no unresolved conflicts on the trunk), (b) `jj log -r 'default@' --no-graph -T 'conflict ++ \"\\n\"'` shows no conflicts, and (c) the suite is green at the trunk (`cargo test` passes — you may run it once; it should be under ~60s). If ANY check fails, STOP and report \"NOT SETTLED\" (under 50 words) — the master will re-schedule. Only when settled, proceed to STEP 1.\n\nSTEP 1 — READ THE SPEC: open /Users/charliesmith/code/deploy/local/task/1.txt and implement EXACTLY what it specifies (a major redesign: exclusive target OWNERSHIP per slot (`target = \"production\"` + `groups = [...]`) replacing `targets: Vec<String>`, the `--group <name>` CLI on push/status, normalized SlotSelection, partial-rollout snapshot semantics with complete per-success target snapshots, target-lock concurrency with selected-slot remote locks, one-owning-target observed state without cross-target propagation, owning-target rotation without unioning, target-wide checkpoint/log, removal of the obsolete `targets:` machinery, schema bumps rejecting old records, and the NINE listed property tests). Implement it in your workspace per the jj-parallel-features + rust skill conventions (own workspace, JJ_EDITOR=true commits, no rebase/bookmark touches, never leave uncommitted work; the task file's \"Do not weaken\" list is a hard constraint — atomic remote activation, content/release identity verification, compensation, path containment, subprocess deadlines+reaping, durable pre-mutation intent, exact historical physical-binding validation).\n\nSTEP 2 — GATE + COMMIT: cargo build 0 warnings; cargo test ×2 green; cargo fmt --check clean; cargo clippy --all-targets 0 warnings. Commit with a message like \"state: exclusive target ownership + rollout groups (SlotSelection, --group, partial-rollout snapshots)\" (the spec's acceptance criteria: update requirement.md + README/help + examples + scaffolding).\n\nREPORT BACK (under 250 words): the settlement verdict; final jj change ID; the SlotSelection shape; the partial-snapshot overlay + guards; the observed/rotation simplification; the schema bumps; the nine properties' status; cargo test counts (×2) + clippy + fmt.",
      "isolated": false,
      "enabled": true,
      "createdAt": "2026-08-25T16:14:38.473Z",
      "runCount": 0
    }
  ]
}